const state = {
  drawer: null,
};

const getters = {};

const actions = {};

const mutations = {
  setDrawer(state, val) {
    state.drawer = val;
  },
};

export default {
  state,
  getters,
  actions,
  mutations,
};
